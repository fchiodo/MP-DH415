# Trading Bot Dependencies
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
slack_sdk>=3.21.0
certifi>=2023.0.0

# API Server (for React UI)
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# FXCM ForexConnect API
# Linux/Windows: installed via pip (works on GCP e2-micro)
# macOS ARM64: use local .whl instead
#   pip install forexconnect-1.6.43-cp310-cp310-macosx_11_0_arm64.whl
forexconnect>=1.6.0

# Note: MetaTrader5 is Windows-only
# For Windows production:
#   pip install MetaTrader5
